/* 
    FUNCTIONS

    -Chunk of code that does set actions when we call it
    -Can call them anytime we want, and pass them any data we want
*/

//function declaration
function hi() {
    console.log('hello')
}

hi() //function must be called by its name if we want the function to run
